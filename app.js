const NAluno = "arthur toninho"
const idade = "16"
const matriculado = false;

if (matriculado == true) {
    const matricula = "SIM";
    console.log(`O aluno ${NAluno} tem ${idade} anos. Status de matricula ${matricula}`);
} else {
    const matricula = "NÃO";
    console.log(`O aluno ${NAluno} tem ${idade} anos. Status de matricula ${matricula}`);
}